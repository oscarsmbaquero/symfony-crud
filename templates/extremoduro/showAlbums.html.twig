
    {% extends 'extremoduro/baseExtremoduro.html.twig' %}
        
          {% block contenido %}
                    <div class="container">
                        <div class="row">
                            <div class="col">
                                <img src="{{ album.image }}">
                            </div>
                            <div class="col text-center">
                                <h2 class="h2">{{ album.name }}</h2>
                                <ul>
                                    {% for song in songs %}
                                        <li><h2 class="h2">{{song.name}}</h2></li>
                                    {% endfor %}
                                </ul>
                            </div>
                        </div>
                    </div>
            {% endblock %}
            